% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_line_sample_to_points_pro.R
\name{st_line_sample_to_points_pro}
\alias{st_line_sample_to_points_pro}
\title{Sample Points Along a Linestring Geometry}
\usage{
st_line_sample_to_points_pro(sf_object, samp_dist = 100)
}
\arguments{
\item{sf_object}{An `sf` object containing LINESTRING geometries.}

\item{samp_dist}{A numeric value specifying the sampling distance in meters. Defaults to 100.}
}
\value{
An `sf` object containing POINT geometries, sampled at the specified distance,
with the original attributes and an added `index` column.
}
\description{
This function takes an `sf` object with LINESTRING geometries and generates
sampled points along each linestring at a specified distance.
It retains the original attributes from the input `sf` object and adds a new `index` column.
}
\details{
This function uses `st_line_sample` to sample points along each LINESTRING geometry,
transforms the geometries to EPSG 4326, and retains the non-geometry attributes of the input.
The resulting points are assigned a sequential `index` for identification.
}
\examples{
\dontrun{
# Convert LineString to sampled points
sampled_points <- st_line_sample_to_points_pro(line_string_data, samp_dist = 100)
}
}
